import '../scss/style.scss';

/**
 * Fires the event
 */
const p28SearchForm = document.getElementById('searchForm');

if (p28SearchForm) {
    p28SearchForm.addEventListener('submit', onSearchFormChange, false);
}

function onSearchFormChange() {
    e.preventDefault();
    const data = {

    }
}